# People Bundles

Each person gets their own bundle directory:

- `profile.json`
- `projekte.json`
- `cert.json`
- `portrait.jpeg` or another supported portrait filename

The CLI selects a bundle with `--person <person_id>`.

Current starter bundle:

- `toni`
- `john-smith`
